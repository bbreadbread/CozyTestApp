<UserControl x:Class="CozyTest.ForShellWindow.CreateEditParticipantAdmin"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CozyTest.ForShellWindow"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
  mc:Ignorable="d" 
  HorizontalAlignment="Stretch"  VerticalAlignment="Center" Margin="50">
    <StackPanel Orientation="Vertical">
        <TextBlock Text="Работа с пользователем" Margin="20" Grid.ColumnSpan="3" Style="{StaticResource ShellTitle}"/>
        <StackPanel>
            <StackPanel.Resources>
                <Style TargetType="TextBox" BasedOn="{StaticResource ShellTextBox}">
                </Style>
            </StackPanel.Resources>
            <TextBox Grid.Row="0" mah:TextBoxHelper.Watermark="Имя Фамилия" Text="{Binding NameParticipant}" />
            <TextBox Grid.Row="2" mah:TextBoxHelper.Watermark="Логин" Text="{Binding LoginParticipant}"/>
            <TextBox Grid.Row="4" mah:TextBoxHelper.Watermark="Пароль" Text="{Binding PasswordParticipant}"/>

            <StackPanel Orientation="Vertical" Grid.Column="0" Grid.Row="6" VerticalAlignment="Center">
                <RadioButton GroupName="123" Content="Закрепить за мной" FontSize="24" IsChecked="{Binding BindMe}"/>
                <StackPanel Orientation="Horizontal"  Visibility="{Binding Converter={StaticResource PasswordEditVisibilityConverter}, ConverterParameter=Admin}">
                    <RadioButton GroupName="123" Content="Закрепить за " FontSize="24" IsChecked="{Binding BindFor}"/>
                    <ComboBox Width="305"
                              Style="{StaticResource UpMenuTestComboBox}" 
                              FontSize="24" 
                              ItemsSource="{Binding CuratorsList}" 
                              DisplayMemberPath="Name"
                              SelectedItem="{Binding SelectedCuratorForBind}"/>
                </StackPanel>
                <RadioButton GroupName="123" Content="Не закреплять ни за кем" FontSize="24" IsChecked="{Binding BindNone}" />
            </StackPanel>

            <Button Grid.Row="8" Style="{StaticResource ShellButton}" Content="Сохранить" FontSize="30" Command="{Binding SaveParticipantCommand}"/>

        </StackPanel>
    </StackPanel>
</UserControl>
