<Controls:MetroWindow x:Class="CozyTest.ClosedWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CozyTest"
        mc:Ignorable="d"
        Title="ClosedWindow" Height="200" Width="400"
        xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
        WindowTitleBrush="{StaticResource DarkSwamp}"
        GlowBrush="{StaticResource DarkSwamp}"
        BorderBrush="{StaticResource DarkSwamp}"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Style="{StaticResource Hint}" HorizontalAlignment="Left"  Grid.ColumnSpan="2" >
            <Run Text="{Binding WarningText1}"/>
        </TextBlock>

        <TextBlock Grid.Row="1" Style="{StaticResource Hint}" HorizontalAlignment="Left"  Grid.ColumnSpan="2">
            <Run Text="{Binding WarningText2}"/>
        </TextBlock>

        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center">
            <Button Style="{StaticResource MiniHomeButton}" Margin="0 0 15 0" VerticalAlignment="Bottom" x:Name="BtnExit" Grid.Row="3" Grid.ColumnSpan="2" Content="Выход" Click="ButtonExit_Click" />
            <Button Style="{StaticResource MiniHomeButton}" Margin="15 0 0 0" VerticalAlignment="Bottom" x:Name="BtnCancel" Grid.Row="3" Grid.ColumnSpan="2" Content="Отмена" Click="ButtonCancel_Click" />
        </StackPanel>
    </Grid>
</Controls:MetroWindow>
