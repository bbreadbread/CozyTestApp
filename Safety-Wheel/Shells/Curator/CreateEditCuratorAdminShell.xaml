<UserControl x:Class="CozyTest.ForShellWindow.CreateEditCuratorAdminShell"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CozyTest.ForShellWindow"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
      mc:Ignorable="d" 
       Height="Auto" Width="Auto" Margin="80">
    <StackPanel Orientation="Vertical">
        <Grid>
            <Grid.Resources>
                <Style TargetType="TextBox" BasedOn="{StaticResource ShellTextBox}">
                </Style>
            </Grid.Resources>

            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="80"/>
                <RowDefinition Height="40"/>

                <RowDefinition Height="80"/>
                <RowDefinition Height="40"/>

                <RowDefinition Height="80"/>
                <RowDefinition Height="20"/>

                <RowDefinition Height="40"/>
                <RowDefinition Height="80"/>
            </Grid.RowDefinitions>

            <TextBox Grid.Row="0" Margin="0 0 0 0" mah:TextBoxHelper.Watermark="Имя Фамилия" Text="{Binding NameCurator}" />
            <TextBox Grid.Row="2" Margin="0 0 0 0" mah:TextBoxHelper.Watermark="Логин" Text="{Binding LoginCurator}"/>
            <TextBox Grid.Row="4" Margin="0 0 0 0" mah:TextBoxHelper.Watermark="Пароль" Text="{Binding PasswordCurator}"/>

            <TextBlock Grid.Row="6" Margin="0 0 0 0" Text="При создании куратора к нему привязывается одноименная страница в роли тестируемого" TextElement.FontSize="18" HorizontalAlignment="Center"/>
            <Button Grid.Row="7" Margin="0 0 0 0" Style="{StaticResource ShellButton}" Content="Сохранить" FontSize="30" Command="{Binding SaveCuratorCommand}"/>

        </Grid>
    </StackPanel>
</UserControl>
