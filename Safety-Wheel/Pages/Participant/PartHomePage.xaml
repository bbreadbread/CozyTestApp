<Page x:Class="CozyTest.Pages.Participant.PartHomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CozyTest.Pages.Participant"
       xmlns:conv="clr-namespace:CozyTest.Converters"
      mc:Ignorable="d" 
               HorizontalAlignment="Stretch"
         VerticalAlignment="Stretch">

    <Page.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
        <conv:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibility"/>
    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid>
            <ScrollViewer Margin="40" Grid.Row="0" 
                      HorizontalAlignment="Center" 
                      VerticalAlignment="Center" 
                      VerticalScrollBarVisibility="Hidden"
                      HorizontalScrollBarVisibility="Disabled">
                <Grid>
                    <ItemsControl ItemsSource="{Binding Tests}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border MouseLeftButtonUp="Card_Click"
                                        Tag="{Binding}"
                                        Style="{StaticResource CardBorderStyle}">
                                    <Grid>
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="2*"/>
                                                <RowDefinition Height="2"/>
                                                <RowDefinition Height="*"/>
                                            </Grid.RowDefinitions>

                                            <Grid Grid.Row="0">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="*"/>
                                                    <RowDefinition Height="*"/>
                                                    <RowDefinition Height="*"/>
                                                </Grid.RowDefinitions>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="1"/>
                                                    <ColumnDefinition Width="3*"/>
                                                </Grid.ColumnDefinitions>

                                                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 0 25 0" Grid.Row="0" Grid.Column="0">
                                                    <TextBlock Text="{Binding Test.DTestType.Name}" FontSize="20" FontWeight="Bold"/>
                                                </StackPanel>

                                                <Border BorderBrush="Black" BorderThickness="2" Margin="0 10" Grid.Column="1"/>

                                                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 0 25 0" Grid.Row="0" Grid.Column="2">
                                                    <TextBlock Text="{Binding Test.Topic.Name}" FontSize="20" FontWeight="Bold"/>
                                                </StackPanel>

                                                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="1" Grid.ColumnSpan="3">
                                                    <TextBlock Text="{Binding Test.Name}" FontSize="32"/>
                                                </StackPanel>

                                                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="2" Grid.ColumnSpan="3">
                                                    <TextBlock Text="{Binding Test.Description}" FontSize="15" TextWrapping="Wrap" MaxWidth="360"/>
                                                </StackPanel>

                                            </Grid>

                                            <Border Grid.Row="1"
                                                BorderBrush="{StaticResource LightGray}"
                                                BorderThickness="2"/>

                                            <Grid Grid.Row="2">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="2*"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>

                                                <Grid.RowDefinitions>
                                                    <RowDefinition/>
                                                    <RowDefinition/>
                                                </Grid.RowDefinitions>

                                                <TextBlock Grid.Column="0" Grid.Row="0"
                                                       Text="{Binding Test.DateOfCreating, StringFormat={}{0:dd.MM.yyyy}}"
                                                       VerticalAlignment="Center"  FontSize="20" Margin="30 0 0 0"/>

                                                <TextBlock Grid.Column="1" Grid.Row="0"
                                                       Text="{Binding Test.CuratorCreate.Name}" VerticalAlignment="Center"  FontSize="20" Margin="30 0 0 0"/>

                                               
                                                <!--<Button Grid.Column="1"
                                                    Grid.Row="1"
                                                    Tag="{Binding Test}"
                                                    Click="RemoveTest_Click"
                                                    Width="50"
                                                    Height="50"
                                                    HorizontalAlignment="Right"
                                                    Background="Transparent"
                                                    BorderThickness="0"
                                                    Padding="0">

                                                    <Image Source="pack://application:,,,/Images/delete_icon.png"
                                                       Width="50"
                                                       Height="50"/>
                                                </Button>-->
                                            </Grid>
                                        </Grid>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
            </ScrollViewer>
            <Grid Background="#80000000"
              Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}"
              Panel.ZIndex="100">

                <StackPanel VerticalAlignment="Center"
        HorizontalAlignment="Center">

                    <ProgressBar Width="200"
             Height="20"
             IsIndeterminate="True"/>

                    <TextBlock Text="Загрузка..."
           Foreground="White"
           FontSize="16"
           Margin="0,10,0,0"
           HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Page>
